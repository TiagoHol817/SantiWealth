/**
 * Trii broker parser — stub. Same rationale as toro.ts: until we have
 * sample screenshots, the generic parser is more reliable than guessing.
 */
import type { OcrPosition } from '../../types'
export function parseTrii(_rawText: string): OcrPosition[] {
  return []
}
